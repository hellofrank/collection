<p>装修知识</p>
<ul>
	<% @articles.each do |article| %>
		<li><%= link_to article.title, article %>|<%= article.created_at.year %>-<%= article.created_at.month %>-<%= article.created_at.day %></li>
	<% end %>
</ul>
